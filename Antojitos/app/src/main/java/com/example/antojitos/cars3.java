package com.example.antojitos;

import androidx.appcompat.app.ActionBar;
import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.widget.TextView;

public class cars3 extends AppCompatActivity {

    TextView contador ;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_cars3);

        //para mostrar el  numero de registros de pedidos pre ordenados
        //consulta no de pedidos
      //  contador = (TextView) findViewById(R.id.textGridcont);

       // contador.setText("100");


        //para mostrar el  numero de registros de pedidos pre ordenados
    }
}